import { Typography } from "@mui/material";
import { theme } from "@theme";
import styled from "styled-components";

export const GreyText900 = styled(Typography)`
  && {
    color: ${theme.grey[900]};
    font-size: 1.4rem;
  }
`;

export const GreyText800 = styled(Typography)`
  && {
    color: ${theme.grey[800]};
    font-size: 1.4rem;
  }
`;

export const GreyText600 = styled(Typography)`
  && {
    color: ${theme.grey[600]};
    font-size: 1.4rem;
  }
`;
